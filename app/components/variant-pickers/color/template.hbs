<h4>{{theme.label}}</h4>

{{#each values as |value|}}
  {{variant-pickers/-private/item value
      selected=_selectedValues
      class="color-picker--item"
      on-change=(action variant-selected)
  }}
{{/each}}
